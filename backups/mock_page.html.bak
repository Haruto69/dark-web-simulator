{% extends "base_stub.html" %}
{% block content %}
  <div class="card slim" style="border-left:4px solid var(--neon);margin-bottom:12px;">
    <strong>SIMULATION NOTICE:</strong> This is a training page only. No real offers, vendors, or contact details are provided.
  </div>

  <article class="card">
    <h2>{{ page.title }}</h2>
    <p class="muted">{{ page.content }}</p>

    <div style="margin-top:14px;">
      <a class="btn" href="{{ url_for('phishing_consent') }}">Attempt Purchase (Simulated)</a>
    </div>
  </article>
{% endblock %}
